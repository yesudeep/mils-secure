/*var gentium_font_spec = {
    fontFamily: 'Gentium'
};
var frutiger_font_spec = {
    fontFamily: 'Frutiger LT Std Light'
};
var cosmetica_font_spec = {
    fontFamily: 'MgOpen Cosmetica'
};
var button_font_spec = {
    fontFamily: 'MgOpen Cosmetica'
};

function render_fonts(){
    Cufon.replace('#header h1.title', cosmetica_font_spec);
    Cufon.replace('#header h2.subtitle', gentium_font_spec);
    Cufon.replace('p', gentium_font_spec);
    Cufon.replace('a.button.green', frutiger_font_spec);
    Cufon.replace('form span', frutiger_font_spec);
    Cufon.replace('form legend', frutiger_font_spec);
    // Required for IE.
    Cufon.now();
}

// Do not make this call inside a document ready handler.
render_fonts();
*/
function render_fonts(){
    
}